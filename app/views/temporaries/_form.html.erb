<%= form_for(@temporary) do |f| %>
  <% if @temporary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temporary.errors.count, "error") %> prohibited this temporary from being saved:</h2>

      <ul>
      <% @temporary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
